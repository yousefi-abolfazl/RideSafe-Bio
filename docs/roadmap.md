# RideSafe-Bio — نقشه راه و زمان‌بندی

**آخرین بازنگری:** 2026-09-08
**نسخه:** 1.0 (2026-09-08) — فازبندی مطابق رودمپ مصوب کارفرما.

## نمای کلی

| فاز | عنوان | خروجی کلیدی | پیش‌نیاز | وضعیت |
|---|---|---|---|---|
| ۱ | زیرساخت، معماری و مستندسازی | مخزن گیت + venv + مستندات چهارگانه | — | ✅ انجام شد |
| ۲ | پیش‌پردازش سیگنال و داده سنتتیک | فیلتر 5 Hz + `synthetic_gen.py` + ۴ دیتاست مرزی | ۱ | 🔄 در جریان (۲.۱ ✅) |
| ۳ | هسته ISO 17929 | `iso17929_engine.py` مستقل (Jerk، پالس/دوز، بیضی ۳بعدی، RB) | ۲ | ⬜ |
| ۴ | داشبورد Streamlit | UI بارگذاری/تنظیمات، نمودارهای Plotly، شناسنامه ریسک، گزارش | ۳ | ⬜ |
| ۵ | یکپارچگی 17842 و مقایسه | `iso17842_engine.py` + `comparator.py` + تب مقایسه | ۳ (دسترسی به کد مهندس حکمت) | ⬜ |

## فاز ۱ — زیرساخت ✅

| تسک | محتوا | وضعیت |
|---|---|---|
| ۱.۱ | `git init` + `.gitignore` پایتون | ✅ |
| ۱.۲ | درخت پوشه: `data/`, `docs/` (۴ سند), `src/`, `tests/`, `app.py` | ✅ |
| ۱.۳ | venv + `requirements.txt` (numpy, scipy, pandas, streamlit, plotly) | ✅ |

**معیار اتمام فاز ۱:** مخزن عملیاتی با commit اولیه، محیط مجازی با پکیج‌های نصب‌شده و صحت‌سنجی import، مستندات چهارگانه در `docs/`.

## فاز ۲ — Data Engine (در جریان)

| تسک | محتوا | معیار پذیرش | وضعیت |
|---|---|---|---|
| ۲.۱ | فیلتر پایین‌گذر Butterworth ۴قطبه، fc = 5 Hz (الزام ASTM F2137 / Table B.1 Note 3) | پاسخ فرکانسی تأییدشده، ورودی/خروجی یکسان‌طول — ۱۴ آزمون pytest پاس | ✅ |
| ۲.۲ | `src/synthetic_gen.py`: سینوسی، ذوزنقه‌ای، اسپایک گذرا، نویز گاوسی | هر شکل‌موج با پارامترهای دامنه/مدت/نرخ پیکربندی‌پذیر — ۲۲ آزمون pytest پاس | ✅ |
| ۲.۳ | ۴ دیتاست در `data/`: `data_safe_family.csv`، `data_jerk_violation.csv` (نرخ > 15 g/s)، `data_3d_combined_violation.csv` (نقض فقط بیضی سه‌محوره)، `data_cumulative_dose_violation.csv` (نقض مساحت B.15) | هر دیتاست دقیقاً یک قاعده را نقض کند و بقیه را پاس کند | ⬜ |

## فاز ۳ — هسته ISO 17929

| تسک | محتوا |
|---|---|
| ۳.۱ | ماژول Jerk: da/dt پس از فیلتر 5 Hz؛ حدود 7 g/s خانوادگی / 10 g/s عمومی / 15 g/s extreme |
| ۳.۲ | ماژول پالس: تفکیک خیز/پلاتو/افت، مساحت هر پالس، دوز تجمعی طبق B.15 |
| ۳.۳ | بیضی سه‌محوره: (ax/ax,adm)² + (ay/ay,adm)² + (az/az,adm)² ≤ 1.0 |
| ۳.۴ | رده‌بندی RB-1..RB-4 از Table B.1 + الزامات مهار متناظر |

## فاز ۴ — داشبورد

| تسک | محتوا |
|---|---|
| ۴.۱ | UI: بارگذاری CSV/TXT یا انتخاب سنتتیک؛ تنظیمات وارونگی/نام محورها |
| ۴.۲ | Plotly: سیگنال فیلترشده سه‌محوره با سایه‌زنی نقض‌ها؛ پراکندگی ۳بعدی بیضوی |
| ۴.۳ | پنل شناسنامه ریسک: Badge های RB-1..RB-4 + الزامات مهار |
| ۴.۴ | Export گزارش خلاصه برای بازرس میدانی |

## فاز ۵ — Dual-Standard

| تسک | محتوا |
|---|---|
| ۵.۱ | `src/iso17842_engine.py`: قطعه‌بندی bin، کنترل مدت (g ≤ L با Tmax)، بیضی‌های جفتی |
| ۵.۲ | `src/comparator.py`: ماتریس مقایسه روی داده یکسان + کشف نقاط کور استاندارد قدیم |
| ۵.۳ | تب «مقایسه تطبیقی دو استاندارد» در داشبورد |

## ریسک‌ها و وابستگی‌ها

| ریسک | اثر | پاسخ |
|---|---|---|
| متن WIP 17929 ناقص است (شکل‌های B.17/B.23 موجود نیستند) | خطوط تحمل +Z/−Z از رئوس پاکت‌های گسسته بازساخت می‌شوند | مستندسازی بازساخت + برچسب «بازسازی‌شده»؛ پیکربندی‌ماندن اعداد |
| تعارض 17929 و 17842 | حکم دوگانه | حاکمیت 17842 + گزارش هر دو (A9) |
| تأخیر دسترسی به کد مهندس حکمت | فاز ۵ عقب می‌افتد | فاز ۵ مستقل از ۲–۴ طراحی می‌شود؛ رابط موتور از ابتدا یکسان نگه داشته می‌شود |

## مدیریت تحویل

هر فاز با مرور معیارهای پذیرش و به‌روزرسانی `task_log.md` بسته می‌شود؛ commit های گیت به‌صورت قاعده‌مند به تسک‌ها لینک می‌شوند.
