# RideSafe-Bio — لاگ روزانه پیشرفت تسک‌ها

> هر ردیف: تاریخ — فاز/تسک — شرح — نتیجه — گیت (hash).

| تاریخ | تسک | شرح | نتیجه | Commit |
|---|---|---|---|---|
| 2026-09-08 | ۱.۱ | `git init` (شاخه `main`) + `.gitignore` پایتونی با استثناهای پروژه (`.obsidian/`، PDFهای استاندارد) | ✅ | `89ad444` |
| 2026-09-08 | ۱.۲ | ساخت درخت پوشه `data/`, `docs/` (۴ سند), `src/`, `tests/` | ✅ | — |
| 2026-09-08 | ۱.۳ | venv (Python 3.14) + `requirements.txt` (numpy, scipy, pandas, streamlit, plotly, pytest) + نصب و صحت‌سنجی import + تست دود فیلتر Butterworth | ✅ | — |
| 2026-09-08 | ۱.۲ | نگارش مستندات چهارگانه: `project.md`, `roadmap.md`, `rules.md`, `task_log.md` | ✅ | — |
| 2026-09-08 | — | نمونه سنتتیک `app.py` و `src/__init__.py` به‌عنوان داربست قابل اجرای فاز ۱ | ✅ | — |

## یادداشت‌ها
- `pandas 3.0.5` نصب شد — APIهای سنتی `.append` و مانند آن در فازهای بعد قابل استفاده نیستند.
- فیلتر فاز ۲ با `scipy.signal.butter(4, 5.0, btype='low', fs=fs)` پیاده‌سازی خواهد شد؛ تست دود در همین فاز انجام شد.
- ابهام‌های باز استاندارد (A1–A14) در `02-phase-zero.md` باقی است؛ موارد مؤثر بر کد در پیکربندی فاز ۳ قابل تغییر نگه داشته می‌شود.

## گام بعدی
فاز ۲ — تسک ۲.۱: ماژول پیش‌پردازش (فیلتر 5 Hz) در `src/`.
