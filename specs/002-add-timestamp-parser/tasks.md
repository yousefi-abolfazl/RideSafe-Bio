---

description: "Task list for feature 002-add-timestamp-parser (generated by /speckit.tasks)"
---

# Tasks: Add Timestamp Parser (clock-time → seconds)

**Input**: Design documents from `/specs/002-add-timestamp-parser/`

**Prerequisites**: plan.md ✅ · spec.md ✅ (incl. Clarifications 2026-09-19) · research.md ✅ · data-model.md ✅ · contracts/ ✅ · quickstart.md ✅

**Tests**: INCLUDED — FR-009 mandates failing-first deterministic tests; every story phase below starts with its test tasks.

**Organization**: grouped by user story (US1 P1 → US2 P2 → US3 P3); single-project layout; all parser tests live in `tests/test_preprocessing.py` per the `test_<module>.py` convention (convention section 7).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: can run in parallel (different files, no dependencies)
- **[Story]**: owning user story (US1/US2/US3)
- Exact file paths included in every description

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: governance registration and a verified starting point

- [x] T001 Register the failing-first task plan (Goal / Checkpoints / Changes skeleton) for feature 002 in `docs/task_log.md` under the Phase 5 section, per convention section 2
- [x] T002 [P] Verify pre-change baseline by running `.venv/bin/python -m pytest tests/ -q` and confirming 128/128 green before any edit

**Checkpoint**: governance entry exists; baseline proven green.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: shared fixtures and a pinned existing behavior — MUST be complete before any user story

- [x] T003 Add shared reference-data fixtures to `tests/test_preprocessing.py`: resolver for the seven study recordings listed in `specs/002-add-timestamp-parser/contracts/test-contract.md` plus a module-level pytest skip when `Acceleration effects on passengers/Datasets_acc_safe/data/` is absent, so a fresh clone stays green
- [x] T004 Pin existing numeric-passthrough behavior (test contract T1): a numeric-seconds time column passes through `standardize_signal_frame` unchanged with metadata `time_format_detected="seconds"` — must pass BEFORE and AFTER all changes (`tests/test_preprocessing.py`, FR-005)

**Checkpoint**: fixtures ready; current behavior pinned. User story implementation may begin.

---

## Phase 3: User Story 1 — Load logger files without touching the time column (Priority: P1) 🎯 MVP

**Goal**: a `HH:MM:SS.mmm` time column (with or without the fused trailing counter) becomes a numeric seconds timeline through the standard ingestion path, and the full evaluation runs end-to-end.

**Independent Test**: load one reference recording via `standardize_signal_frame` with the caller-supplied mapping → numeric seconds axis starting at 0, metadata `time_format_detected="clock_hhmmss"`, evaluation completes (quickstart.md steps 4–5).

### Tests for User Story 1 ⚠️ write first, observe FAIL

- [x] T005 [US1] Failing-first conversion test (test contract T2): leading tokens `16:06:01.213 12533899` (fused counter) and bare `16:06:01.224` both convert to `0.000 / 0.011 / …` with millisecond precision and first value 0 in `tests/test_preprocessing.py` (FR-001, clarification 2026-09-19)

### Implementation for User Story 1

- [x] T006 [US1] Implement the pure helper `convert_clock_timestamps(stamps, rollover_threshold_s=60.0)` in `src/preprocessing.py` per `specs/002-add-timestamp-parser/contracts/api-contract.md`: vectorized detection (finite number → seconds path; clock grammar `^HH:MM:SS(.mmm)?(\s+\d+)?$` → conversion; else error), float seconds relative to first stamp, no I/O, no globals (research D2/D3/D8)
- [x] T007 [US1] Integrate the helper into `standardize_signal_frame` in `src/preprocessing.py`: clock-string time columns convert + stable sort; numeric columns untouched; metadata gains `time_format_detected`, `rows_in`, `rows_out`, `tied_stamps`, `rollover_threshold_s`, `time_column` — existing keys unchanged (api-contract behavior table, FR-005/FR-007)
- [x] T008 [US1] Metadata-presence test: both paths (numeric + clock) expose every contract metadata key after conversion in `tests/test_preprocessing.py` (FR-007)
- [x] T009 [US1] Integration test (test contract T10): سورتمه run-1 reference recording flows standardize → `apply_butterworth_lowpass` → `calculate_jerk_rate` without error; skips gracefully when data absent (`tests/test_preprocessing.py`, FR-008, US1.1)

**Checkpoint**: MVP — real logger recordings load and evaluate through the standard path with zero manual timestamp editing.

---

## Phase 4: User Story 2 — Trustworthy timeline on messy serial data (Priority: P2)

**Goal**: out-of-order rows, tied stamps, and millisecond jitter are handled deterministically with provable row integrity.

**Independent Test**: the coast_r2-style recording with repeated and backward-stepping stamps converts to a non-decreasing timeline with exactly preserved row count and reported tie count (quickstart.md step 4 checks).

### Tests for User Story 2 ⚠️ write first, observe FAIL

- [x] T010 [US2] Failing-first integrity tests (test contract T3/T4): shuffled rows → non-decreasing output with count preserved; two equal stamps → both kept in file order with `tied_stamps == 1` in `tests/test_preprocessing.py` (FR-003/FR-004, US2.1/US2.2)
- [x] T011 [US2] Failing-first jitter test (test contract T5): a −8 ms backwards step sorts away with zero rows lost in `tests/test_preprocessing.py` (FR-003)

### Implementation for User Story 2

- [x] T012 [US2] Implement stable ordering in `src/preprocessing.py`: stable sort on converted time (file order breaks ties), tie counting reported in metadata — no row dropped, merged, or invented (research D4, FR-003/FR-004)
- [x] T013 [US2] Reference-data integrity test (test contract T11): `rows_out == rows_in` for all seven recordings, blank lines excluded, timeline starts at 0 (`tests/test_preprocessing.py`, SC-002)

**Checkpoint**: timelines provably intact on the real recordings.

---

## Phase 5: User Story 3 — Clear refusal instead of a wrong verdict (Priority: P3)

**Goal**: malformed, broken, mixed, and rollover inputs fail loudly with line-numbered messages; nothing bad reaches a verdict.

**Independent Test**: three crafted bad files + one rollover file each raise the specific `ValueError` of the api-contract error table (quickstart.md step 6).

### Tests for User Story 3 ⚠️ write first, observe FAIL

- [x] T014 [US3] Failing-first refusal tests (test contract T7/T8/T9): unparseable stamp → error with 1-based line number + expected grammar; mixed numeric/clock column → error; structurally broken line → error with line number (`tests/test_preprocessing.py`, FR-006)
- [x] T015 [US3] Failing-first rollover test (test contract T6): a backwards jump of hours is refused with the threshold in the message; a custom `rollover_threshold_s` override is honored (`tests/test_preprocessing.py`, FR-006)

### Implementation for User Story 3

- [x] T016 [US3] Implement strict error paths and thread `rollover_threshold_s` through `standardize_signal_frame` in `src/preprocessing.py`: first malformed line aborts with line number; sorted-order backward jump > threshold aborts with both stamps and threshold; blank lines skipped (research D5/D6, api-contract error contract)

**Checkpoint**: bad data can no longer reach a verdict.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: performance proof, regression proof, governance, cleanup

- [x] T017 Performance smoke (SC-003): converting the largest reference file (~48k rows) completes under 2 s inside the suite with a tolerant assertion in `tests/test_preprocessing.py`
- [x] T018 Run the full regression gate (SC-004/FR-009): `.venv/bin/python -m pytest tests/ -q` — existing 128 tests plus all new tests green; the suite's existing UI-free check on `src/` still passes
- [x] T019 [P] Documentation: complete the Task 5.x log entry (Goal/Checkpoints/Changes/Result-Validation) in `docs/task_log.md`, add the ingestion gap note to `docs/roadmap.md` phase 5, and mark feature 002 done
- [x] T020 Retire the temporary diagnostic script `scripts/evaluate_hekmat_samples.py` after acceptance — its behavior is now covered by the suite (spec Assumptions)
- [x] T021 Commit the feature on branch `002-add-timestamp-parser` as `feat: parse clock timestamps to seconds in ingestion` (code + tests + specs + docs) and attempt push to `origin` per convention section 10

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: none — start immediately
- **Foundational (Phase 2)**: after Setup — BLOCKS all stories (T003 fixtures used by T009/T013/T017; T004 pin guards every later change)
- **US1 (Phase 3)**: after Foundational — implements the conversion every other story builds on
- **US2 (Phase 4)**: after US1 (T007) — extends ordering/integrity of the same helper
- **US3 (Phase 5)**: after US1 (T007) — adds refusal paths around the same helper
- **Polish (Phase 6)**: after all stories

### User Story Dependencies

- **US1**: no story dependencies (only Foundational)
- **US2**: depends on US1's helper + integration (T006/T007)
- **US3**: depends on US1's helper + integration (T006/T007)
- US2 and US3 are logically independent of EACH OTHER, but both edit `src/preprocessing.py` and `tests/test_preprocessing.py` — run sequentially in priority order to avoid same-file conflicts

### Within Each User Story

1. Story tests written first and observed FAILING
2. Implementation in `src/preprocessing.py`
3. Remaining story tests → green
4. Checkpoint validation before the next story

### Parallel Opportunities

- T001 ∥ T002 (docs vs command)
- T019 ∥ T017/T018 (documentation vs code/tests)
- Test tasks within one story intentionally share `tests/test_preprocessing.py` (module convention) — sequential by design; parallelism across stories requires separate worktrees and is not recommended for this single-file feature
- All [P] markers follow the strict rule (different files, no dependencies)

---

## Implementation Strategy

### MVP First (User Story 1 only)

1. Phase 1 → Phase 2 → Phase 3
2. **STOP and VALIDATE**: run quickstart.md steps 4–5 — a real سورتمه/صاعقه file loads and evaluates untouched
3. Ship-worthy state: engineers can already assess real recordings

### Incremental Delivery

- +US2 → timeline integrity provable on messy data → validate
- +US3 → bad data refused loudly → validate
- Polish → performance, regression, governance, cleanup

### Single-Developer Strategy (this project)

Strict sequential order T001→T021; commit at story checkpoints (after Phase 3, 4, 5) plus the final commit T021.

---

## Notes

- Failing-first is mandatory: T005, T010, T011, T014, T015 must be observed failing before their implementations (T006, T012, T016)
- No new dependencies, no config keys, no `app.py` logic — the clarification keeps the counter tolerance inside the core grammar
- Tests depending on untracked study data must skip cleanly on a fresh clone (T003)
